{
  "name": "minecraft-launcher",
  "version": "1.0.0",
  "description": "Aureate Launcher with C++ Core",
  "main": "src/ui/main.js",
  "scripts": {
    "start": "electron .",
    "build": "cmake -B build -S . && cmake --build build --config Release",
    "rebuild": "npm run build",
    "dev": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "build": {
    "appId": "com.aureate.launcher",
    "productName": "Aureate Launcher",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "!src/core/**/*.cpp",
      "!src/core/**/*.h",
      "!**/*.map"
    ],
    "extraResources": [
      "build/Release/minecraft_core.node"
    ],
    "win": {
      "target": "nsis",
      "icon": "src/ui/assets/icon.ico"
    }
  },
  "dependencies": {
    "electron": "^25.0.0",
    "fs-extra": "^11.1.1",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "electron-builder": "^24.6.4",
    "node-gyp": "^9.0.0"
  },
  "gypfile": true
}
